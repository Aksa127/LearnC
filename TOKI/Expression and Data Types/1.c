#include <stdio.h>

int input;
int arr;

int main()
{
    for (int i = 0; i < 2; i++)
    {
        scanf("%d", &input);
        arr[i] = input:
        printf("%d\n", input);
        printf("%d\n", arr[i]);
    }

return 0;
}